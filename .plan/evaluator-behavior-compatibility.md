# Evaluator behavior compatibility plan

## Non-negotiable constraints

- Preserve all JVM obfuscation invariants from `AGENTS.md`: no fallback behavior, no reduced transform coverage, no reflection/lambda special-case deobfuscation, no plaintext reflective application data, no static key exposure, and no skip-on-error path.
- Use fresh artifacts only. Do not rely on stale `build/test-jvm-full-obf-perf/` evaluator outputs as proof.
- Do not use Recaf. Use `javap`, ASM helper programs, JVM exception logging, and repository tests/CLI outputs.
- Do not touch unrelated current native-code/test work shown by `git status --short`.
- Do not create files or folders under `/tmp`.

## Current evidence

- The full JVM obfuscation gate currently obfuscates `test-jars/evaluator-unobf.jar` but does not execute it in `JvmFullObfuscationPerfTest`.
- User reports evaluator behavior inconsistency after the reflection/lambda compatibility repair.
- The repository Gradle wrapper has already been approved for this repair stream.

## Subtasks

[x] EVAL-1: Regenerate fresh evaluator artifacts and compare behavior

- Scope: Freshly obfuscate evaluator with `test-jars/full-jvm-obf.yml`, identify the evaluator entry point and required runtime invocation, then run original and full-obfuscated evaluator artifacts under the same inputs. Do not modify transform code in this subtask.
- Required evidence: Fresh obfuscation command output, original/full-obfuscated stdout/stderr/exit code, exact input arguments or stdin used, and exact first behavioral divergence.
- Validation command/runtime target: Use repository `./gradlew` or installed CLI from a fresh test run to generate `build/test-jvm-full-obf-perf/evaluator-full-jvm-obf.jar`, then run original and obfuscated evaluator with identical JVM args and inputs.
- Completion criteria: A concrete evaluator divergence is reproduced from fresh artifacts and recorded without relying on stale output.
- Completion evidence: Fresh full-obf artifacts were regenerated with `env GRADLE_USER_HOME=.gradle-user-home ./gradlew :neko-test:test --rerun-tasks --tests dev.nekoobfuscator.test.JvmFullObfuscationPerfTest`, which completed with `BUILD SUCCESSFUL in 13s`. The evaluator entry point is the jar manifest main class `dev/sim0n/evaluator/Main` (`a/a` after renaming). Running `java -XX:-UsePerfData -jar test-jars/evaluator-unobf.jar` completed successfully through annotations, AES, Blowfish, large-string comparison, and final decrypt output. Running the fresh `build/test-jvm-full-obf-perf/evaluator-full-jvm-obf.jar` before the evaluator fix exited 1 after `Loaded 4 tests` with `BootstrapMethodError` caused by `LambdaConversionException: Invalid receiver type long; not a subtype of implementation type interface a.r`.

[x] EVAL-2: Localize evaluator root cause

- Scope: Map the divergent evaluator behavior to transformed class/method/descriptor/callsite/key path using `javap`, ASM inspection helpers, and JVM exception/logging output. Do not implement until the failing invariant is proven.
- Required evidence: Exact transformed method/site, generated bytecode or metadata proving the incorrect invariant, and source transform owner responsible for the defect.
- Validation command/runtime target: Focused original/full-obf evaluator reruns plus targeted disassembly/ASM dumps of the affected classes.
- Completion criteria: Root cause is generic and architecture-level, not evaluator-specific.
- Completion evidence: `javap -classpath test-jars/evaluator-unobf.jar -c -p -v dev.sim0n.evaluator.manager.TestManager dev.sim0n.evaluator.test.Test` shows `handleTests()` uses an unbound `LambdaMetafactory` method reference for `Test::handle`: bootstrap method argument `REF_invokeInterface dev/sim0n/evaluator/test/Test.handle:()V`, instantiated method type `(Ldev/sim0n/evaluator/test/Test;)V`. The failing obfuscated artifact maps `Test -> a/r` and implementations `AnnotationTest/OpaqueConditionTest/WeirdLoopTest/StringTest -> a/t/a/v/a/w/a/x`. Before the fix, `javap -classpath build/test-jvm-full-obf-perf/evaluator-full-jvm-obf.jar -p a.r a.t` showed `a/r.n(J)V` while an implementation such as `a/t` exposed `public void n(Object[])`; the `TestManager` bootstrap used `REF_invokeInterface a/r.n:(J)V` with instantiated type `(La/r;)V`. That ABI mismatch proves a generic unbound instance method-reference bug: key-dispatch treated the interface SAM as a lambda target and inserted a hidden long parameter, while method-parameter packing did not rewrite the interface implementation family to the same lambda ABI. The source owners are `JvmKeyDispatchPass.indySamTargets` and `JvmMethodParameterObfuscationPass.indyHandleTargets`, which only protected the lambda return SAM signature and direct bootstrap handle, not the virtual implementation family of unbound instance method references.

[x] EVAL-3: Patch generic JVM transform compatibility

- Scope: Fix the proven transform invariant at the source while preserving CFF, method-parameter carriers, hidden keys, invokedynamic, string/constant obfuscation, and renaming strength. No fallback, skip, helper-layer bypass, or plaintext exposure.
- Required evidence: Source-level mapping from the failing transformed site to the generic transform change.
- Validation command/runtime target: Run the smallest focused command that exercises the changed evaluator path, then run the full JVM obfuscation gate.
- Completion criteria: Fresh obfuscated evaluator matches original behavior under the recorded inputs and no forbidden weakening is introduced.
- Completion evidence: The generic fix teaches both key-dispatch and method-parameter planning to recognize unbound non-static LambdaMetafactory handles (`captured argument count == 0`) as virtual-family SAM surfaces. `JvmKeyDispatchPass.indySamTargets` now adds every in-program subtype implementation of the handle owner/name/descriptor to the SAM exclusion set, preventing hidden-key ABI insertion on the interface method and its implementation family. `JvmMethodParameterObfuscationPass.indyHandleTargets` now applies the same virtual-family exclusion to prevent Object[] packing from splitting that family away from the LambdaMetafactory contract. This preserves invokedynamic obfuscation and CFF for the callsite while leaving the JVM SAM ABI valid; it does not add fallback, skip behavior, helper-layer bypass, or plaintext reflective data.

[x] EVAL-4: Final evaluator parity and checkpoint

- Scope: Regenerate the full JVM obfuscation artifacts, run evaluator original/full-obf parity, run related full-obf fixtures, update this plan with evidence, and commit only evaluator-fix source plus this plan update.
- Required evidence: Fresh evaluator parity logs, full-obf test command output, source audit for forbidden fallback markers, and git commit hash.
- Validation command/runtime target: `env GRADLE_USER_HOME=.gradle-user-home ./gradlew :neko-test:test --rerun-tasks --tests dev.nekoobfuscator.test.JvmFullObfuscationPerfTest` plus the focused evaluator parity command.
- Completion criteria: All evaluator behavior differences are resolved or proven to be identical to the original fixture behavior; committed checkpoint exists.
- Completion evidence: Fresh full JVM obfuscation gate after the fix completed with `BUILD SUCCESSFUL in 13s`, regenerating report timestamp `2026-05-15T05:18:59.949719752Z`. The focused evaluator parity commands wrote `build/inspect/evaluator-original-after-fix.stdout/.stderr` and `build/inspect/evaluator-full-obf-after-fix.stdout/.stderr`; both original and full-obf evaluator runs exited 0 and reached the final lines `Successfully compared strings` and `Successfully decrypted hello world 123 1605479835458`. `javap -classpath build/test-jvm-full-obf-perf/evaluator-full-jvm-obf.jar -p a.r a.t` now shows the SAM ABI is coherent: `a/r.n()V` and `a/t.public void n()`. Compatibility/performance checks for the other full-obf artifacts were also run: TEST full-obf repeated runs kept Counter/Loader/ReTrace/Annotation passing and only retained the original Java 21 `Sec ERROR`; obfusjack full-obf repeated runs completed with `=== All tests completed ===`. A fresh comparison worktree at pre-fix commit `d905007` showed no performance regression from this fix in the full-obf gate: TEST Calc improved from previous `215ms` to current `200ms`, obfusjack Seq improved from previous `349ms` to current `335ms`, and current repeated obfusjack Seq median was `338ms` with compatibility green. SnakeGame remains in the non-interactive group; the full-obf gate freshly obfuscated it and accepted its hardening audit, with output size `319187` bytes versus pre-fix `321483` bytes.

## Performance target extension

Target requested after evaluator compatibility validation: keep the repaired evaluator behavior and bring the freshly generated full-obf performance gate to TEST `Calc <= 180ms` and obfusjack matrix `Seq <= 300ms`, while preserving all non-negotiable obfuscation constraints above.

[x] PERF-1: Diagnose hot-path overhead against fresh artifacts

- Scope: Use fresh generated TEST/obfusjack full-obf jars and non-Recaf inspection (`javap`, ASM helpers, JVM runtime output) to identify whether the evaluator compatibility patch itself widened generated hot-path overhead in the other fixtures. Do not modify CFF or any non-evaluator-fix surface.
- Required evidence: Exact generated method/site or transform invariant causing repeated runtime overhead, current runtime measurements, and proof whether the diagnosis is evaluator-, TEST-, or obfusjack-specific.
- Validation command/runtime target: Current full JVM obfuscation gate plus focused repeated runs of `build/test-jvm-full-obf-perf/TEST-full-jvm-obf.jar` and `build/test-jvm-full-obf-perf/obfusjack-full-jvm-obf.jar`.
- Completion criteria: The evaluator-fix surface is either narrowed to remove its cross-fixture effect or proven not to be the remaining source of hot-loop overhead.
- Completion evidence: Non-Recaf ASM scan of the original fixtures found exactly one in-program unbound LambdaMetafactory virtual family requiring the evaluator fix: `dev/sim0n/evaluator/manager/TestManager.handleTests()V -> dev/sim0n/evaluator/test/Test.handle()V` with five in-program family members. `TEST.jar` and `SnakeGame.jar` have no such sites. `obfusjack-test21.jar` has one unbound site, `java/util/random/RandomGeneratorFactory.isJumpable()Z`, but its owner is external and the in-program family count is zero. The evaluator fix was narrowed so virtual-family expansion only runs for bootstrap handles whose owner is present in `pctx.classMap()`, removing the no-op external-family scan from obfusjack while keeping the evaluator repair. Fresh full-obf gate after that narrowing passed at report timestamp `2026-05-15T05:34:08.297740675Z`; TEST Calc was `192ms` and obfusjack Seq was `344ms`, so the requested `180ms/300ms` target remains unmet by changes limited to the evaluator-fix surface.

[ ] PERF-2: Implement generic performance repair

- Scope: Make one coherent source change that reduces the proven hot-path overhead without disabling CFF, hidden keys, parameter packing, invokedynamic, string/constant obfuscation, reflection/lambda compatibility rewrites, or renaming.
- Required evidence: Source-level link from the failing overhead invariant to the transform/runtime code change; no fallback, skip, helper-layer bypass, static key exposure, or plaintext reflective data.
- Validation command/runtime target: Rebuild with `env GRADLE_USER_HOME=.gradle-user-home ./gradlew :neko-test:test --rerun-tasks --tests dev.nekoobfuscator.test.JvmFullObfuscationPerfTest`.
- Completion criteria: Fresh full-obf artifacts are regenerated after the change and the affected generated sites show the intended lower-overhead form.

[ ] PERF-3: Validate compatibility and target performance

- Scope: Run full JVM obfuscation gate and focused repeated runtime checks for evaluator, TEST, and obfusjack; compare against the requested targets and the pre-fix comparison evidence.
- Required evidence: TEST Calc repeated median/max relevant to `<= 180ms`, obfusjack Seq repeated median/max relevant to `<= 300ms`, evaluator parity success, TEST compatibility lines, obfusjack completion lines, and source audit for forbidden fallback markers.
- Validation command/runtime target: Full gate plus focused `java -XX:-UsePerfData -jar ...` runs for evaluator, TEST, and obfusjack.
- Completion criteria: `TEST Calc <= 180ms` and `obfusjack Seq <= 300ms` on fresh repaired artifacts, with compatibility still green and checkpoint commit created.
